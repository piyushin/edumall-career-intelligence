{
  "name": "@edumall/eslint-config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared ESLint flat config for the EduMall monorepo.",
  "type": "module",
  "exports": {
    "./base": "./base.mjs",
    "./next": "./next.mjs"
  },
  "peerDependencies": {
    "@eslint/js": "^9.33.0",
    "eslint": "^9.33.0",
    "eslint-config-prettier": "^10.1.8",
    "typescript-eslint": "^8.39.1"
  },
  "scripts": {
    "build": "node -e \"console.log('eslint-config has no build step')\"",
    "lint": "node -e \"console.log('eslint-config lints consumers')\"",
    "test": "node -e \"console.log('eslint-config has no tests')\"",
    "test:unit": "node -e \"console.log('eslint-config has no unit tests')\"",
    "type-check": "node -e \"console.log('eslint-config has no type-check step')\""
  }
}
